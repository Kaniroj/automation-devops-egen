name: Test and Build

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run test
        run: |
          echo "Running tests..."
          python -c "assert 1 + 1 == 2"
          echo "Tests passed!"

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build app
        run: echo "Bygger appen eftersom testet gick bra!"
